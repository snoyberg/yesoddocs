$(function(){
  $("#accordion").accordion({
    collapsible:true,
    autoHeight: false,
    active: false,
    change: function(event, ui){
      var href = "";
      ui.newContent.children("a.ajax").each(function(ignored, a_){
        var a = $(a_);
        href = a.attr("href");
      });
      if (href != "") {
        ui.newContent.html("<div id='loading'>Loading</div>");
        $.get(href, function(data) {
          $('#loading').html(data);
        });
      }
    }
  });
});
